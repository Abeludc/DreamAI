// Índice principal del tema para DreamAI
import colors from './colors';
import typography from './typography';
import spacing from './spacing';

const theme = {
  colors,
  typography,
  spacing,
  // Configuración para modo oscuro (predeterminado)
  dark: true,
};

export default theme;
